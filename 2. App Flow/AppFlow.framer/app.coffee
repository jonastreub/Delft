# 1. Create a Frame (shortcut: F)
# 2. Add a header and tab bar, with title and tab icons
# 3. Create a card showing a header image and title (use assets)
# 4. Duplicate the card three times and fill with data
# 5. Switch over to code and add a Flow component
# 6. Target the frame and show it inside the Flow component, (showNext)
# 7. Switch back to Design and create a second frame for the navigation
# 8. Target the hamburger icon and show the navigation on click
